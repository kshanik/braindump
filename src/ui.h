#ifndef _UI_H
#define _UI_H

#include <iostream>
#include <map>
#include <fstream>
#include <string>

typedef struct {
    std::string text;
} ui_text;

void render_text(ui_text text)
{
}

void draw_rect(vec2 xy, vec2 wh)
{
}

#ifdef _UI_IMPLEMENTATION


#endif //_UI_IMPLEMENTATION

#endif //_UI_H
